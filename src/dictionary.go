package main

/*func getDictionary() {
	file, err := os.Open("dictionary.txt")
	if err != nil {
		fmt.Println(err)
	}
	defer file.Close()

	dict := make([]byte, 64)

	for {
		n, err := file.Read(dict)
		if err == io.EOF {
			break
		}
		fmt.Print(string(dict[:n]))
	}
}*/

/*func getWord() {
	fmt.Println("Тут будет получение слов.")
}
*/
